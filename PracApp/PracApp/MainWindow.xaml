<Window x:Class="PracApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PracApp"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        Closing="Window_Closing" WindowStyle="None" AllowsTransparency="True">

    <Grid>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition Width="90"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0" >
                
            </Grid>
            <Grid Grid.Column="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0">
                    <Button x:Name="MiniButt" Click="MiniButt_Click" Content="-"></Button>
                </Grid>
                <Grid Grid.Column="1">
                    <Button x:Name="MaxOrNormAppButt" Click="MaxOrNormAppButt_Click" Content="O"></Button>
                </Grid>
                <Grid Grid.Column="2">
                    <Button x:Name="CloseAppButt" Click="CloseAppButt_Click" Content="X"></Button>
                </Grid>
            </Grid>
        </Grid>

        <Grid Grid.Row="1">
            
        </Grid>

        
        
        
        <tb:TaskbarIcon 
            x:Name="TrayIcon"
            IconSource="C:\Users\Anton\Pictures\Castle.png"
            ToolTipText="Мое приложение">
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Развернуть" Click="RestoreWindow_Click"/>
                    <MenuItem Header="Выйти" Click="Exit_Click"/>
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>
    </Grid>
</Window>